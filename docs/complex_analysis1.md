
# Complex Analysis

```py
	복소수 생성	z = 2 + 3j 또는 z = complex(2, 3)
실수부/허수부 접근	z.real, z.imag
절대값 (크기)	abs(z)
켤레 복소수	z.conjugate()
NumPy	복소수 배열 생성	arr = np.array([1+2j, 3-4j])
기본 연산 (배열)	arr * 2, arr + arr (배열 연산 지원)

```
